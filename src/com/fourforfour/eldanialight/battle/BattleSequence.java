package com.fourforfour.eldanialight.battle;

import com.fourforfour.eldanialight.characters.Boss;
import com.fourforfour.eldanialight.characters.Character;
import com.fourforfour.eldanialight.Main;

import java.util.Scanner;

public class BattleSequence  extends Character{
    Scanner myScanner = new Scanner(System.in);







    /*
   public void enterBattle(){

   sout(" You have entered a battle with " + currentEnemy);
   sout("Do you want to fight or run?")
  String battleChoice = tolower(myScanner.nextline);

  if (battleChoice.equals("run){
  run();
  } else if (battleChoice.equals("fight"){
  fight();
  } else {
  sout("you have entered an invalid action");
  }

   }


    public void setStats{

    int playerHealth = Player.health + Player.defense;
    int enemyHealth = currentEnemy.health + currentEnemy.defense;
    int playerAttackingPower = Player.strength + Player.intel + Player.speed;
    int enemyAttackingPower = currentEnemy.strength + currentEnemy.intel + currentEnemy.speed;

    if(WearItem) {
         playerHealth +=  20;
    }

   if(playerType.equals("mage")){
        playerAttackingPower += (Player.mana * .5)
   }
    }

    public String fight(){
    boolean fightMode = true;

    while(fightMode){

    }
    }



     */
}
